<% if user_signed_in? && current_user.id == @user.id  %>
  <%= link_to 'Edit', edit_post_path(@user.id, @post.id) %>
<% end %>
<h2><%= @post.title %></h2>
<h2><%= @post.task.title %></h2>
<%= image_tag @post.image, class: 'post' %>
